import Button from "./button/index";
import Input from "./input/index";
import TextBox from "./textBox/index";
import HashTag from "./hashTag/index";

const Atoms = {
  Button,
  Input,
  TextBox,
  HashTag
};

export default Atoms;

export { Button, Input, TextBox, HashTag };
