import useColor from "./useColor/useColor";
import useIcons from "./useIcons/useIcons";
import useTheme from "./useTheme/useTheme";

const Hooks = {
  useColor,
  useIcons,
  useTheme,
};

export default Hooks;

export { useColor, useIcons, useTheme };
