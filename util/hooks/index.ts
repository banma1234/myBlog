import useColor from "./useColor/useColor";
import useDate from "./useDate/useDate";
import useIcons from './useIcons/useIcons';

const Hooks = {
  useColor,
  useDate,
  useIcons,
};

export default Hooks;

export { useColor, useDate, useIcons };
